# Blades of Hex V6.0 — 将·领·归·位

V5 发版（12 位新将领预览）→ V6（104 次提交），从预览演进为完整发布：将领技能全部端到端跑通、伤害公式标准化、特效模块化、测试体系全量覆盖。

---

## 🆕 E 系列：四名新将领 + 补员系统

V5 预览了 8 位，V6 新增剩余 4 位并全线跑通：

| 将领 | 技能 | 机制亮点 |
|---|---|---|
| **E1 占星者** | 夜观（被动） + 星移（主动 CD4） | 3 格友军天气免疫；锁定天气 2 回合使敌方双倍负面 |
| **E2 亡灵法师** | 留魂 + 回魂 | 阵亡留亡魂标记（持续 3 回合），诅咒占据者 10% 真伤/回合；回魂牵引魂卒（40%HP/70%ATK），限 2 只 |
| **E3 纵横家** | 合纵 + 连横 | 手牌上限+1、每回合用卡+1；处于非己方行政区内时对方用卡 50% 概率获得复制 |
| **E4 空军上校** | 扫射 + 轰炸 + 空运 | 三张金币消耗空军卡，走上校攻击力管线（含弱点破甲 25%/范围溅射 60%），雾天停飞 |
| **E5 补员系统** | 主动补员按钮 | 城市/村庄主动补员（5 回合冷却），移除旧版自动回血 |

## ⚖️ 伤害公式标准化（B7 重构）

**攻击力 × 增伤 × 浮动 × 防御** 四层乘算：

| 乘区 | 组成 |
|---|---|
| ① 攻击力 | `config.attack × (1 + auraAtk) + _atkBonus + getCommanderAttackBonus()` |
| ② 增伤 | 克制±20% + 魔术师千面+25% + 幻形+5%/层 + 骑兵冲锋+10%/格 + 天气增伤 + 通用空军+35% |
| ③ 浮动 | 浮动区间（含顺克+25%暴/逆克锁暴）+ 堕天使+60%暴 + 幻形+10%/层；无独立暴击判定 |
| ④ 防御 | 地形 + 森林掩蔽 + 天气修正 + 军衔 + 士气 + 将领加成 + 停滞者力场 + 防空层（封顶 2 层×25%）|

覆盖旧版 3 套伤害管线，所有将领加成通过 `getCommanderAttackBonus() / getCommanderDefenseBonus()` 四个出口注入。

## 🏗️ 架构优化

### 将领特效模块化（-1650 行）
- 12 名将领的绘制/更新代码从 `renderer.js` / `effects.js` 剥离到独立 `commander/fx/{id}.js`
- 新增 `fxRegistry.js` 图层注册表 + `commanderFx.js` 按需加载器
  - 每局只动态 import 在场 2~3 名将领的 FX 模块，不选的零帧开销
  - 倒计时 3s 期间并行加载，重连时 `stateSync` 自动恢复
- 9 个 `drawFxLayer` 渲染挂载点覆盖全部注册钩子，管线完整

### 中间层解耦
- 将领钩子通过 `helpers` 调用特效，不再直接操作 effects 数组
- `main.js` 注入所有 ref（`setSpawnFxRef` 等），headless 下自动 no-op

### 确定性 RNG
- 引入 `core/rng.js`：战斗/卡牌/将领/天气掷骰统一走 `gameState.rng`
- 状态随 `serializeState` 同步，联机收方与重连方骰子序列对齐

### 防空火力通用化
- 提炼 `getAALayers` / `applyAADefense` / `applyAADropHP` 三个接口
- 全图防空网显示，空袭/空降卡均受防空影响

## 📜 16 名将领先驱完整技能

| 将领 | 核心机制 | 来源 |
|---|---|---|
| 谋士 | 受击 75% 攻心→士气下降/感化 | 原创 |
| 铁卫 | 120 盾 + 每回合回 40 + 灵光友军伤害转移 | 原创 |
| 吸血鬼 | 攻击回血 30%-60%，溢出 50%→护盾（上限 60） | 经典 |
| 停滞者 | 2 格缚足 + 远程压制 + 力场防御+25% | 原创 |
| 百夫长 | 攻击 30%/击杀必定 +3MP 再行动（每回合 1 次） | 经典 |
| 尚书 | 驻扎城市 = 每回合 $1×回合数（上限 $12） | 原创 |
| 殉道者 | HP≤1 殉道 2 格 AOE；己方阵亡永久+5ATK（上限 25） | 原创 |
| 狂战士 | 每损 2%HP → ATK/DEF 各+1%（上限 40%） | 经典 |
| 堕天使 | 白=回血 30%，黑=ATK+30/暴率+60%/流失 20% | 原创 |
| 魔术师 | 克制+25%/-15%；击杀变形 + 幻形叠层（6 层上限） | 原创 |
| 圣骑士 | 灵光 ATK+10% + 士气免疫；誓言叠层+5%DEF；至圣斩真伤 | 经典 |
| 牧师 | 圣链群体回血（10%+5%）；祈祷=消耗 50%HP 附治愈灵光 | 经典 |
| 占星者 | 3 格天气免疫；星移锁定天气 2 回合（CD4） | **E1** |
| 纵横家 | 手牌+1/用卡+1；敌区 50% 复制对方用卡 | **E3** |
| 亡灵法师 | 亡魂诅咒 10% 真伤；回魂魂卒（40%HP/70%ATK） | **E2** |
| 空军上校 | 3 种金币消耗空军卡（扫射/轰炸/空运），走上校攻击力管线 | **E4** |

## 🧪 全量测试套件

```
node test/run-all.mjs --quick    # ≈ 2 分钟
```

| 套件 | 覆盖 | 断言 |
|---|---|---|
| `static` | 全量语法检查 + ESM 交叉审计 | 2 |
| `pve` | 单机完整对局（~19 轮至胜利） | 16 |
| `net` | 联机 2 轮 + 断线重连 + stateSync 恢复 | 19 |
| `cmdr` | 16 名将领技能钩子 + 中间层 + 序列化往返 | 189 |
| `fx` | 12 个 FX 模块注册验证 | 26 |
| **总计** | **231 项 0 失败** | |

## 🎨 视觉

- **至圣斩三段递进**：剑出鞘 → +220ms 天降金色光束 → +420ms 扩散环+强震+真伤数字
- 勇气灵光环 + 环绕剑（对应誓言层数）+ 圣链 + 统御环 + 血怒辉光等全部保留
- 每局按需加载：只渲染在场将领的特效

## 🖼️ 性能

- 将领立绘全量转 WebP：**74.6 MB → 6.5 MB（-91%）**
- 每帧无意义条件判断减少约 70%-80%
- renderer.js 精简约 1200 行

## 🔧 其他修复

- 上校空军伤害走 `_colUnit._resolveDamage(...)` + 触发 `reapColonelKill` 士气/经验
- 占星者星移 `lastWeather` 污染修复 + unit 引用悬空修复
- 纵横家连横拷贝移到 `broadcastAction` 前执行（远端同步修复）
- 空袭/空降飞机动画重复播放/透明度残留修复
- 三人模式重连顶栏信息卡恢复 + 角色错配阵营修复
- 移动端大厅页面裁剪修复
- 联机大厅准备计数异常 + 三人模式通知缺陷修复
- server.js 多余花括号删除
- F12 console 日志精简
- net.test.mjs 修复（随机角色分配 + 回合轮转等待 + 重连流程）
