# AI 重写验收 · 共享引擎时代第一轮（2026-07-24/25）

承接《真人首局复盘与AI优化方向-2026-07-24.md》。本轮按该文的规划彻底重写了三个玩家 AI 脚本：旧的三份巨石脚本（imperator 2679 行 / legatus 523 / optio 269，参数互搏、各自漂移）推翻，换成「一个共享引擎 + 三个薄 profile」。旧脚本备份在 `backup/ai-legacy-2026-07-24/`，可随时回滚或对照。

## 新架构（ai/core/）

一条管线，七个分层模块，三档共用：

- `perceive.js` 感知层：世界模型（威胁图、行政区资产估值、迷雾情报龄期、胜利时钟）。
- `strategize.js` 战略层：姿态状态机（expand/contest/defend/recover + **hold 守钟 / allin 死斗**）。
- `missions.js` 任务层：跨回合存续的攻城/守备/截杀/侦察编组（占领者 + 护航显式绑定）。
- `tactics.js` 战术层：决斗期望定价（近战贴脸 = 缠斗承诺）、集火协调、破门序列（先软化、终结者收尾进城）、无人机作战。
- `cards.js` 卡牌层：带预算与姿态政策的剧本（守钟只留对策卡给占城者、死斗全砸夺城链）。
- `production.js` 生产层：突击兵缺口 → 腾位招陆军；补员台账（同单位最多 2 次）。
- `weights.js` 唯一允许"拍脑袋"的地方：评分量纲统一为等效金币；档位能力档（噪声、威胁外推、情报、任务持久化、卡牌剧本、胜利时钟、攻心路径、运输规划）。

档位差异 = 信息深度，不是三份代码。`ai/doctrine.js`（规则事实）与 `ai/strategy.js`（已测纯函数）原样保留复用。

## 方法论教训（本轮最重要的工程结论）

1. **benchmark 必须固定种子**：早期每轮换随机种子，70%→30% 的波动全是种子噪声。`tools/runBenchmark.mjs --seed-prefix=xxx` 固定 10 局矩阵（双地图×标准/迷雾×单双将×2P/3P，困难档轮换座位），改动对比才有意义。
2. **一次只改一个变量**：集结机制/任务滞后/支援火力等"合理"改动单独测全是负收益，已逐一回滚。任何打包改动都无法归因。
3. **档位共享引擎抬高所有船**：中档变强后，困难档胜率被"档差"压缩——档差主要来自困难档独占能力（威胁外推、任务持久化、胜利时钟、运输、攻心、侦察）。
4. **看门狗决定下限**：真人对战暴露了旧版 18 秒固定看门狗每回合截断 AI（"AI对手超时，强制结束回合"），等于让 AI 残废出战。动态看门狗（`estimateAiTurnWatchdogMs`，上限 90 秒）是任何智能的前提。

## 50 局对照（同 5 组随机种子，困难 vs 中/低档）

| 指标 | 旧脚本 | 新引擎（最终） |
| --- | --- | --- |
| 胜率 | 46%（23胜11平16负） | **54% 留出验收（27胜4平19负）；固定种子峰值 62%（31胜9平10负）** |
| 场均攻击 | 50.6 | ~58（+15%） |
| 场均击杀 | 14.9 | ~15（持平，质量升：亏本交换占比 <8%） |
| 场均占城 | 2.0 | ~1.5-2.2（任务化攻城，替人拆门≈0） |
| 平局占比 | 22% | 8%（残局死斗/守钟生效，不再靠回合限制数城平局） |

胜率读数方法：每个改动先在 2-3 组固定种子上对比，过线后跑 5 组全新留出种子取最终数。
固定种子组曾到 62%，留出组回落到 54%——方向正确但未达 60% 验收线，差距主要在
阵亡控制（场均阵亡仍比旧脚本高约 15%）与硬种子场景（王冠环岛多方混战局）。

## 真人第二局录像（crown-ring-mrznqu37-0ftdfk）的整合结论

真人 9 回合 18:4 歼灭旧 AI（旧 AI 每回合被 18 秒看门狗截断，只打出残血回合）。整合项：

- 动态看门狗（已在工作区）是 P0 修复，已验证必要性；上限 90→120 秒，演出系数 1.35→1.6。
- 陆战图港口招募海军闸门（js/ai.js）：旧 AI 在王冠环岛连招 2 艘潜艇。
- 天眼无人机接入 AI（js/ai.js 运行时 + tactics.planDroneOps）：真人的无人机自爆破防线是教科书操作。
- 将领偏好加入 tianyan / engineer；将领挂舰区分地图（陆战图挂军舰 −140，载具偏好生效）——
  由第三局真人录像（mrzra6w2-spqw36，吸血鬼挂巡洋舰）直接驱动。
- `meta.difficultyId` 缺失曾导致选将页难度映射失效，已补回三档 meta。
- **部署链路的 dist 陷阱**：dist/ 在 .gitignore 中，服务器只 `git reset` 不会更新 bundle，
  前两局真人实际打的是旧 bundle。AGENTS.md 的部署函数已补 `npm run build`（服务器端构建）。

## 迭代史摘要（10 局矩阵 benchmark，全史见 git）

| 阶段 | 关键改动 | 效果 |
| --- | --- | --- |
| 新引擎初版 | 管线 + 金币量纲 | 30%（各处互搏） |
| 战术纪律 | 决斗期望、守军对峙、守备只防"能占城的" | 60%（10 局） |
| 任务系统 | 任务停滞系数、登陆窗口、集结（回滚）、滞后（回滚） | 50-70% 波动 |
| 终局 | finish→守钟/死斗、3P 制衡目标 | 平局减半 |
| 感知诚实 | 迷雾敌力下限（零接触≠敌亡）、兵力趋势转防守 | rndB 30→50% |
| 任务放弃权 | 6 回合无进展且守备占优 → 黑名单换目标 | rndA 50→80%，合计 56% |
| 近战兑现力 | 任务放弃评估只数能进城的近战 | **合计 62%（峰值）** |
| 留出没顶 | 中立支援火力（回滚）、编队保持（回滚）、王牌截杀（限兵力充裕） | 留出验收 54% |

## 人性化评审结论（子代理逐局打分）

宏观层已像人：姿态切换（守钟布雷补员、死斗全线压上）、任务编制（占领者+护航）、
卡牌不烂手、替人拆门基本绝迹。微观执行仍是短板（评审原话"半个会玩的人"）：
宣言死斗但到位率不足、对中立火力圈的孤军深入、终局情报断供。
本轮修复后（任务放弃权+迷雾诚实+近战兑现力），评审指出的三个"最不像人瞬间"
（潜艇排队送岸防炮、攻城挂 18 回合零执行、闪电替人开门）均已消除。

## 当前遗留（按优先级）

1. 阵亡控制：场均阵亡仍偏高，孤军深入中立火力圈是主因；两次"支援火力"尝试均因
   扩张窒息回滚，需要更精确的时机判断（不是更保守，是更准）。
2. 硬种子场景：王冠环岛 2P 多方混战（中立+敌对双线）死亡率仍高。
3. 攻心夺城、运输抢滩已实现但未在 benchmark 中形成稳定收益，待验证。
4. 60% 验收线：留出 54%，差 6 个点；继续按"固定种子验证 + 留出复核"推进。
